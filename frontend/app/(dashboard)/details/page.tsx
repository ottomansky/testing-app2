'use client'

/*
 * DETAILS PAGE
 *
 * Header + NavTabs are in the shared layout — don't import them here.
 */
export default function DetailsPage() {
  return (
    <div className="bento-grid">
      <div className="span-12 placeholder-card min-h-[400px]">Data Table Area</div>
    </div>
  )
}
